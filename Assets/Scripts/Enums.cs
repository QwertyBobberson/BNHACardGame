using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Enums
{
    /*
     * All costum enums needed for this game
     */

    //Possible teams
    public enum Teams { Human, Computer}
    public enum AttackType { Blunt, Sharp, Elemental}
    public enum AttackElement { Fire, Ice, Water, Electricity, Sound, Light, NULL}
    public enum BloodType { A, B, AB, O}
}
